# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: 17_Expect_Assertions\02_Expect.spec.ts >> 2 Locator-based assertions
- Location: tests\17_Expect_Assertions\02_Expect.spec.ts:3:6

# Error details

```
Error: expect(locator).toContainText(expected) failed

Locator: getByText('multiple element filters', { exact: true })
Expected substring: "Filter"
Received string:    "multiple element filters"
Timeout: 5000ms

Call log:
  - Expect "toContainText" with timeout 5000ms
  - waiting for getByText('multiple element filters', { exact: true })
    13 × locator resolved to <em>multiple element filters</em>
       - unexpected value "multiple element filters"

```

```yaml
- emphasis: multiple element filters
```

# Test source

```ts
  1  | import { test, expect} from '@playwright/test'
  2  | 
  3  |  test('2 Locator-based assertions', async ({ page }) => {
  4  | 
  5  |         await page.goto('https://app.thetestingacademy.com/playwright/multiple_element_filter.html');
  6  | 
  7  |         const heading = await page.getByText('multiple element filters',{exact:true});
  8  |         await expect(heading).toBeVisible();
> 9  |         await expect(heading).toContainText('Filter',{timeout:5000});
     |                               ^ Error: expect(locator).toContainText(expected) failed
  10 | 
  11 |         const email = await page.getByRole('textbox', {name:'email'});
  12 |         await expect(email).toHaveAttribute('id', 'email');
  13 |         await expect(email).toHaveAttribute('name', 'email');
  14 |         await expect(email).toHaveAttribute('type', 'email');
  15 |         await expect(email).toHaveAttribute('placeholder', 'student@thetestingacademy.com');
  16 | 
  17 |         const footerLinks = page.locator('footer a');
  18 |         await expect(footerLinks).toHaveCount(16);
  19 | 
  20 |         
  21 | 
  22 | 
  23 |     });
```